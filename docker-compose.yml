services:
  gotobet-monitor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gotobet-monitor
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./screenshots:/app/screenshots
